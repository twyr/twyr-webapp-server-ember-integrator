{{#liquid-if hasPermission}}
	{{#link-to "profile" class="white-text mr-4"}}
		{{#if (not-eq displayImage '')}}<img src="{{displayImage}}" style="max-width:4rem; max-height:2rem;">{{/if}}
		{{displayName}}
	{{/link-to}}
{{/liquid-if}}
